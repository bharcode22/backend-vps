services:
  backend:
    build:
      context: .
      dockerfile: Dockerfile
    container_name: kasir-backend
    restart: always
    ports:
      - "3001:3001"
    env_file:
      - .env
    volumes:
      - ./uploads:/app/uploads
